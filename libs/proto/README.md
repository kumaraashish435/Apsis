# Status: not yet built

**Contains:** shared gRPC/event contracts between `apps/api`, `apps/sim-engine`, and `apps/ai-services`.

**Build trigger:** Created alongside `apps/sim-engine` in Product 4, once a real cross-language service boundary exists. Not needed while propagation stays in-process (Products 1-3).

See `docs/architecture/folder-structure.md` for the full monorepo map and build order.
